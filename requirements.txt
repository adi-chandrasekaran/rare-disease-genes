# Used in notebooks: pandas, matplotlib, seaborn, numpy
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.13.0
numpy>=1.26.0

# Jupyter / VS Code notebook kernel (not imported in code, but needed to run .ipynb)
ipykernel>=6.25.0
